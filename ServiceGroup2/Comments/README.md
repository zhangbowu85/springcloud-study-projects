# Spring Cloud Config Client + Postgresql

in This example:

* use postgresql as the data storage
* use spring cloud config client to use cloud config repository
* use spring eureka client to register service
. 